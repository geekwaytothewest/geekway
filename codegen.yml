overwrite: true
schema: "https://cms.geekway.com/graphql"
documents: "src/**/*.graphql"
generates:
  src/generated/types.graphql-gen.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"